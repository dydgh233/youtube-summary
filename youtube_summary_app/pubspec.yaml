name: youtube_summary_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter  # 디바이스 언어 확인 하는 패키지
  http: ^1.3.0
  flutter_html: ^3.0.0  # 최신 버전
  cupertino_icons: ^1.0.8
  flutter_markdown: ^0.7.7
  flutter_tex: ^4.1.3  # Flutter 3.x와 호환되는 최신 버전
  google_mobile_ads: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
